{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "pip install -r requirements-prod.txt"
    },
    "deploy": {
      "startCommand": "uvicorn app:app --host 0.0.0.0 --port $PORT"
    }
  }